import React from 'react';
import ReactDOM from 'react-dom/client';
import PlanClassifierApp from './PlanClassifierApp.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PlanClassifierApp />
  </React.StrictMode>
);
